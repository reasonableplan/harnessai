{
  "name": "agent-orchestration",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:frontend": "pnpm --filter dashboard-client run dev",
    "build:frontend": "pnpm --filter dashboard-client run build",
    "dev:backend": "cd backend && uv run python -m src.main",
    "test:backend": "cd backend && uv run pytest tests/ -v",
    "test:frontend": "pnpm --filter @agent/dashboard-client test",
    "db:migrate": "cd backend && uv run alembic upgrade head",
    "db:generate": "cd backend && uv run alembic revision --autogenerate -m"
  },
  "packageManager": "pnpm@10.30.3",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
