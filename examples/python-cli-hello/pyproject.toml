[project]
name = "hello-cli"
version = "0.0.1"
description = "Minimal HarnessAI python-cli example"
requires-python = ">=3.12"
dependencies = [
    "click>=8.1",
]

[project.scripts]
hello = "hello_cli.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
